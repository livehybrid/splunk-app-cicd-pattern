# The `python.version` line is the one-liner the AppInspect `future` tag asks
# for. Pinning it here is what stops a platform Python bump surprising you.
[cicdhello]
filename = cicdhello.py
chunked = true
python.version = python3
