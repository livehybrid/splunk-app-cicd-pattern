[cicdhello-command]
syntax = cicdhello
alias =
shortdesc = Emit a single event proving the add-on installed and ran.
description = Generating command that returns one event containing the add-on \
    version and the commit the package was built from. Used as the smoke test \
    for the build, test and ship pipeline.
comment1 = Confirm the add-on is installed and executable.
example1 = | cicdhello
usage = public
